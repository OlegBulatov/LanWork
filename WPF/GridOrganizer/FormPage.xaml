<Page
    x:Class="GridOrganizer.FormPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GridOrganizer">
    
    <Grid>
        <DataGrid HorizontalAlignment="Stretch" Margin="10,10,0,0" VerticalAlignment="Stretch" AutoGenerateColumns="False" ItemsSource="{Binding XObjects}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Тип формы" Binding="{Binding Path=type_name}">
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
